# 产品路线图

本路线图只表达产品阶段和优先级。已合入任务见[里程碑索引](milestones.md)，当前执行顺序和验收边界见[任务清单](todo.md)。

## 阶段状态

| 阶段 | 状态 | 结果或目标 |
| --- | --- | --- |
| P0 基础闭环 | COMPLETED | 管理认证、LLM/语音配置、设备配对、本地唤醒、文字与语音对话、提醒和安全部署边界 |
| P1 陪伴与 Agent | COMPLETED | 人设、长期记忆、主动交互、连续对话、ReactAgent、Skill、Tool、MCP 和个人数据生命周期 |
| P2 运维可靠性 | COMPLETED | 表情资源包、备份恢复、健康中心和安全应用 OTA |
| P3 外部协作 | COMPLETED | 外部通知 REST/MCP、可靠语音投递与通知管理已合入并验收 |
| P3 角色化 | COMPLETED | 多角色容器及人设、会话、记忆、主动主题和提醒隔离已合入并验收 |
| P4 扩展体验 | ACTIVE | 通知摘要、低延迟语音和 MEDIA-002 动态表情已合入；正在评估有限 EAF 生命周期片段 |

## 当前主线

1. `MEDIA-002A/B/C/D`（COMPLETED）：动态球形、完整表情语义、连续帧率、官方 CoreS3 BSP + LVGL 9.4 和实机显示/音量调校已通过并由 `67e5ad3` 合入。
2. `MEDIA-003`（READY_FOR_REVIEW）：默认原生渲染保持不变；官方 EAF player 的项目自有 RLE 开机片段已通过实体语音并发、触摸和稳定性成功路径。`esp_emote_gfx` 不接管显示。
3. `CONN-*`（PAUSED）：日历、任务和天气连接器按用户要求暂缓；恢复时仍需独立授权、撤销和审计。

## 产品边界

- 近期保持单管理员、单长期用户；角色不是多用户权限账户。
- 设备凭据、网络、OTA、音量、免打扰、模型密钥和首版 Skill/MCP 继续共享。
- 本地唤醒和本地 VAD 保持不变，不建设常开云端录音。
- 模型不直接获得数据库、文件系统、凭据或任意副作用权限。
- LAN HTTP 仅用于受信任开发环境；公网生产必须 HTTPS-only。
- 摄像头、舵机、多人识别、插件市场和公网明文接口不进入当前路线。

## 延后事项

- 多自定义外观常驻、GIF 和动态主题编辑器等待 EAF 有限片段实机基准；它们不再阻塞默认动态表情。
- 语音插话要先验证硬件回声消除能力；先交付可取消的有序分段 TTS。
- 宠物形象 AI 生成继续等待素材隐私、删除策略和基准图确认，现有资源包 V1 格式不变。
