# 已完成里程碑

本文只提供已合入能力的短索引。实现细节以对应提交、[架构决策索引](decisions/README.md)和 runbook 为准；活动状态与下一任务分别见[全局总览](status/overview.md)和[任务清单](todo.md)。

## 合并记录

| 阶段 | 合并提交 | 结果 |
| --- | --- | --- |
| 语音、屏保与持久提醒 | `96f3ecc` | 本地唤醒、ASR/TTS、提醒调度和播放 ACK |
| 自定义唤醒词 | `af65290` | 最终收敛为 ESP-SR 内置目录与安全模型 OTA |
| `INT-001` 语音回合诊断 | `ecc40f3` | 端到端阶段时间线和隐私安全元数据 |
| `INT-002` 可见交互状态 | `37dcb49` | 本地八状态显示语义 |
| `INT-003` 触摸控制 | `b65e2b8` | 按住说话、阶段取消和播放停止 |
| `INT-004` 完整语音回复 | `798468d` | 完整回答合成与播放，不截断正文 |
| `INT-005` 人设与长期记忆 | `bfb6a20` | 单例结构化人设和确认后生效的范围记忆 |
| `INT-006` 主动交互 | `9f8e611` | 周期提醒、免打扰、离线策略和有限主动问候 |
| `INT-007` 表情资源包 | `8122959` | 八状态 PNG、同源下载和设备 A/B 回退 |
| `INT-008` Agent/Skill/Tool/MCP | `2a3b712` | 受控 ReactAgent、Skill ZIP、只读 Tool 和授权 MCP Client |
| `DATA-001` 个人数据生命周期 | `a51ac83` | 搜索/导出/物理删除、备份轮转和隔离恢复 |
| `INT-009` 连续对话 | `87a0938` | 本地 VAD 跟进窗口、三回合/两分钟上限 |
| `INT-010` 安全语音操作 | `fbe5bde` | 结构化提案、复述确认和幂等执行 |
| `INT-011` 长期记忆 2.0 | `d7f38bf` | 建议过滤、冲突确认、相关检索和使用记录 |
| `INT-012` 个性化主动关心 | `9377b8d` | 规则门控、单条授权记忆和主题冷却 |
| `OPS-002` 应用 OTA 与健康中心 | `ce8faaa` | V27、应用制品、A/B OTA、健康中心和启动确认 |
| `OPS-002` 传输栈修复 | `0e92d58` | 修复真实 OTA 暴露的 `device_transport` 栈溢出 |
| `EVT-001` 外部通知平台 | `5d18623` | REST/MCP 外部通知、可靠语音投递、令牌与队列管理 |
| `ROLE-001` 角色容器 | `8de06be` | 多角色 CRUD、设备活动角色与完整陪伴数据隔离 |

## 当前已验证运行组合

- Git 基线：`master` 合并提交 `8de06be`。
- LAN server：ROLE-001 server/V29，角色管理和隔离已验收。
- CoreS3：实体验证候选 `7e7c55f`，`motion_disabled`，应用 OTA 能力已启用。
- 实体路径：`cf26fd7 -> 7e7c55f` 应用 OTA、NVS 保留、普通唤醒对话和触摸停止均通过。

运行镜像或设备候选可以不是当前 Git 提交的祖先，因为任务分支在合入前会压缩为单一提交。状态文件必须同时写明 Git 基线与实机版本，不能把两者混作同一标识。

## 历史证据规则

- 已完成任务不再把逐次部署日志复制到活动状态文件。
- 需要调查历史故障时，按上表提交、ADR 编号和 Git 历史定位。
- 真实部署、刷写、备份恢复和人工验收仍记录在对应任务提交及 runbook；任何秘密、音频、转写和完整模型响应不得作为证据保存。
