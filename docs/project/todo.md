# 下一阶段可执行任务清单

- 文档状态：READY_FOR_REVIEW
- 最后更新：2026-08-13
- 实现基准：`5d18623`
- 当前任务：`ROLE-001` 角色容器实现与验证
- 下一任务：`ROLE-002` 角色音色
- 产品边界：单管理员、单用户、软件能力优先

本文只保留当前及后继任务。已完成工作统一查阅[里程碑索引](milestones.md)，当前事实以[全局总览](status/overview.md)和四个工作流状态文件为准。

## Agent 快速接手

1. 按 [`AGENTS.md`](../../AGENTS.md) 顺序阅读入口、全局总览和任务所属工作流状态。
2. 核对状态文件中的基准提交位于当前分支，并识别所有未提交改动。
3. 只选择执行看板中第一项可执行任务，从最新 `master` 创建独立 `codex/` 分支。
4. 先写必要 ADR、协议和 runbook，再实现兼容层、自动化验证和人工验收。
5. 完成时更新状态与总览，压缩为一个中文任务提交；未经明确授权不推送、部署或刷写。

## 固定产品与安全边界

- 机器人服务于一位长期用户；近期不引入多用户权限体系。
- 只有已确认且启用的记忆可进入上下文，角色切换不得扩大记忆范围。
- Agent Tool 和外部通知令牌均采用最小权限；模型不能提供或覆盖设备、角色、会话和授权范围。
- 外部通知不调用 LLM 改写正文，不允许绕过免打扰，不接受任意回调 URL。
- 不开放语音删除、部署、凭据、固件、摄像头或舵机操作，`motion_disabled` 保持不变。
- 生产保持 HTTPS-only；LAN 与 production Compose 不可组合。

## 执行看板

| 顺序 | 任务 | 状态 | 依赖 | 交付结果 |
| --- | --- | --- | --- | --- |
| 0 | `BASE-009` 项目状态整合 | COMPLETED | OPS-002 | 当前状态、里程碑和路线一致，新 Agent 可直接接手 |
| 1 | `EVT-001` 外部通知平台 | COMPLETED | `BASE-009` | REST/MCP、可靠语音投递、令牌轮换和通知管理 |
| 2 | `ROLE-001` 角色容器 | VALIDATING | `EVT-001` 已合入 | 每设备活动角色和完整陪伴数据隔离 |
| 3 | `ROLE-002` 角色音色 | DEFERRED | `ROLE-001` | 角色可选覆盖全局 TTS 音色 |
| 4 | `CONN-*` 日历、任务和天气 | DEFERRED | `EVT-001` | 独立授权、撤销和审计的连接器 |
| 5 | `EVT-002` 互动通知 | DEFERRED | `EVT-001` | 用户回执、稍后处理和摘要聚合 |
| 6 | `INT-013` 低延迟语音 | DEFERRED | 独立协议设计 | 有序分段 TTS、取消和晚到分片丢弃 |
| 7 | `MEDIA-002` 角色形象 | DEFERRED | 多资源包常驻固件 | 不经资源 OTA 的角色表情切换 |

## `BASE-009`：项目状态整合

- 以 OPS-002 合并提交 `0e92d58` 为基线，区分 Git 基线、运行 server 版本和实机固件候选。
- 把活动状态文件压缩为当前事实、最近验证、阻塞和下一条操作；旧部署流水由 Git、ADR、runbook 和里程碑索引承接。
- 路线图和执行看板只保留当前/后继任务，不再重复已完成任务的实施细节。
- 完成结果：路线刷新与完成交接内容已按用户要求并入 EVT-001 的单一任务提交，不再保留独立 BASE-009 提交。
- 下一条精确操作：随 EVT-001 一并审核和人工合入。

## `EVT-001`：外部通知平台

### 交付范围

- 复用提醒调度、TTS、同源音频和设备 ACK；新增 `EXTERNAL` 来源、过期终态、集成归属、幂等键和过期时间。
- 外部通知默认 24 小时过期，离线可靠排队，尊重免打扰和忙碌；每台设备同时最多一个已派发播报。
- 管理员创建绑定单设备的通知集成；令牌只显示一次、仅保存哈希，支持到期、轮换、禁用和撤销。
- 提供带 `Idempotency-Key` 的 REST 创建/查询接口，以及只包含 `push_notification`、`get_notification_status` 的 Streamable HTTP MCP。
- 新增外部通知管理页面和安全健康计数；正文、令牌和认证载荷不进入健康接口或日志。

### 验收边界

- 覆盖撤销/过期令牌、越权、限流、队列上限、幂等冲突、免打扰、忙碌、离线、过期、重启恢复、设备单飞、TTS 失败和 ACK 重放。
- REST 与 MCP 复用同一业务服务；管理员会话、CSRF、现有提醒 CRUD 和 Agent MCP Client 不受影响。
- LAN development 可使用受信任内网 HTTP；公网 production 只允许 HTTPS。
- 实体设备验证在线播报、免打扰延期和离线重连播报；本任务不需要刷写固件。

### 当前完成情况

- V28、独立 Bearer 鉴权、REST、两个 Streamable HTTP MCP Tool、可靠调度、管理页面、健康计数、ADR、协议和 runbook 已完成。
- 服务端 341/341、空库 Flyway V1..V28、真实 MCP HTTP 入队、前端 25 个文件 69/69、类型检查和 production build 已通过。
- 已生成并恢复校验新备份，仅替换 LAN server，运行库成功迁移到 V28；健康接口、前端资源和未认证鉴权边界通过。
- 用户确认基础外部通知测试正常；外部通知菜单已并入“提醒管理”，集成与通知队列已补齐受播放状态保护的删除，服务端 346/346 和前端 69/69/类型检查/构建通过，并已随 `50d6269` 镜像发布。下一条精确操作是复核删除交互，再补验免打扰延期和离线重连。

## `ROLE-001`：角色容器与严格隔离

### 交付范围

- 新增角色和每设备活动角色。角色保存名称、结构化语气/长度/主动程度、受限背景说明、边界、禁忌和归档状态。
- 现有单例人设、会话、记忆、提醒、主动主题和通知集成迁移到默认 `StackChan` 角色。
- 网页会话永久绑定角色；设备语音会话按 `deviceId + roleId` 隔离。模型不能提供或覆盖角色范围。
- 管理页和明确语音动作均可切换角色；语音切换复用两分钟、同设备/同会话、确认后幂等执行的提案机制。
- 默认角色不可归档。归档角色会切回默认角色、取消未来提醒并停用通知集成；历史数据保留只读、可筛选和导出。
- 设备凭据、网络、OTA、音量、免打扰、模型密钥、Skill/MCP、首版音色和表情继续共享。

### 验收边界

- 覆盖默认迁移、网页/设备会话隔离、记忆、主动问候、提醒、Agent Tool、语音切换、归档和个人数据导出。
- 两个角色分别记住不同事实，切换后历史与记忆不串用。
- 本任务不改固件、不切换音色或表情。

### 当前完成情况

- V29 默认迁移、角色服务/API、设备活动角色、归档生命周期、网页/语音会话和全部陪伴数据角色边界已完成。
- 角色管理、设备绑定以及聊天、记忆、提醒、通知集成和个人数据角色筛选已完成；已有数据不可改绑角色。
- 服务端 348/348、空库 Flyway V1..V29、角色/会话定向 13/13，以及前端 69/69、类型检查和 production build 通过。
- 当前分支已部署到 LAN server/V29，未推送、未改固件；下一条精确操作是执行双角色实体隔离验收，验收通过后再获授权推送。

## 每任务统一验证

- 服务端：Maven 全量测试；Testcontainers 从空 PostgreSQL 应用全部 Flyway 迁移。
- 前端：使用满足 engine 的 Node 运行 Vitest、`vue-tsc -b` 和 production build。
- 固件相关：协议测试和目标 profile 构建；未经明确确认不刷写。
- 部署相关：LAN/production Compose 静态检查；未经明确确认不替换运行资源。
- 全部任务：`git diff --check`、`pnpm docs:check`，并更新对应工作流状态和全局总览。
