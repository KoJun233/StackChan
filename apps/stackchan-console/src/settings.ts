import { setSettings } from '@fantastic-admin/settings'

export default setSettings({
  app: {
    home: {
      title: '伙伴首页',
      fullPath: '/dashboard',
    },
  },
  menu: {
    mode: 'single',
  },
})
