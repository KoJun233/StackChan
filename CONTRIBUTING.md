# StackChan 贡献规范

## 分支

- 每个任务从最新的 `master` 创建独立分支。
- 禁止直接在 `master` 上提交、推送或 force push。
- 任务分支可以在开发期间包含多个临时提交，但创建 PR 前必须压缩为一个提交。
- 压缩已推送的任务分支时只能使用 `git push --force-with-lease`，不得使用裸 `--force`。

## 提交信息

提交信息必须使用中文。可以保留 Conventional Commit 的英文类型前缀，但冒号后的标题说明以及正文必须使用中文，例如：

```text
feat: 增加离线提醒补发能力
fix(firmware): 修复唤醒后录音超时
docs: 补充固件刷写验证记录
```

禁止使用只有英文说明的提交信息。PR 标题遵循同一规则。

## Pull Request

1. 将任务分支更新到最新 `master`，解决冲突。
2. 将任务分支的全部改动压缩为一个提交。
3. 对最终提交重新运行任务要求的验证，并更新项目状态文档。
4. 创建目标为 `master` 的 PR，填写变更说明和验证结果。
5. 等待人工审核；PR 创建者和 Agent 不得自行合并。
6. 审核通过后使用 merge commit 合并，保留“一个任务提交 + 一个 PR 合并提交”的历史；不使用 squash merge 或 rebase merge。

GitHub 仓库规则应要求 PR、至少一次有效人工审核、通过 `检查 PR 提交策略` 状态检查，并禁止绕过、force push 和删除 `master`。

> 如果仓库只有一个账号参与，GitHub 不允许 PR 作者批准自己的 PR。要强制“至少 1 个批准”，需要邀请另一位协作者；否则只能强制走 PR，再由仓库所有者人工确认后合并。
